## 📘 Hướng dẫn sử dụng

### 🔹 Bước 1: Tạo môi trường ảo Python và cài đặt thư viện

#### **Windows**

📋 Chạy file batch có sẵn:

```bat
run.bat
```

> File này sẽ tự động:
>
> * Tạo môi trường ảo `venv`
> * Kích hoạt môi trường
> * Cài đặt thư viện từ `requirements.txt`

---

#### 🍎 **macOS / Linux**

📋 Chạy lần lượt các lệnh sau trong Terminal:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

---

### ✅ Sau khi hoàn tất

* Môi trường ảo đã được kích hoạt
* Thư viện cần thiết đã cài đặt xong
* Có thể tiếp tục các bước tiếp theo (migrate, runserver, …)

---


